import 'package:flutter/material.dart';

class StashesPage extends StatelessWidget {
  const StashesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("stashes"),
    );
  }
}

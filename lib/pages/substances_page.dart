import 'package:flutter/material.dart';

class SubstancesPage extends StatelessWidget {
  const SubstancesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("database"),
    );
  }
}
